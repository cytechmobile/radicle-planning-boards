// These columns are required to be present in the board.
// non-planned is the default column for tasks without a label, and done is the default column for closed tasks.
export const requiredColumns = ['non-planned', 'done']

export const initialColumns = ['non-planned', 'todo', 'doing', 'done']
